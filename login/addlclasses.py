class Friend(object):
 	"""docstring for friend"""
 	def __init__(self, fullname,status,imageurl,email):
 		self.fullname = fullname
 		self.status = status
 		self.imageurl = imageurl
 		self.email = email
